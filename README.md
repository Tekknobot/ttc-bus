# ttc-bus
Simple Node server + single HTML page that finds your nearest TTC stop and shows live bus ETAs.

## Run
```bash
npm install
npm start
# then open http://localhost:5173 and allow location
```

Requires Node 18+ (you have Node 24, that's great).
